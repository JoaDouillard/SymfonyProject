{% extends 'base.html.twig' %}

{% block title %}Nouvel artiste{% endblock %}

{% block body %}
    <div class="container">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{{ path('app_home') }}">Accueil</a></li>
                <li class="breadcrumb-item"><a href="{{ path('app_artist_index') }}">Artistes</a></li>
                <li class="breadcrumb-item active" aria-current="page">Nouvel artiste</li>
            </ol>
        </nav>

        <div class="card">
            <div class="card-header">
                <h1>Créer un nouvel artiste</h1>
            </div>
            <div class="card-body">
                {{ include('artist/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
